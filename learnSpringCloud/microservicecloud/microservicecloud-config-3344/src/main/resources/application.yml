server:
  port: 3344

spring:
  application:
    name: microservicecloud-config
  cloud:
    config:
      server:
        git:
          uri: git@github.com:linghongbang/microservicecloud-config.git # gitHbu上面的仓库地址
